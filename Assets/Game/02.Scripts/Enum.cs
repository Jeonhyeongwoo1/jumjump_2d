namespace JumJump
{
    public enum GameStateType
    {
        Ready,
        Playing,
        GameOver
    }

    public enum PlayerStateType
    {
        Idle,
        Jump,
        Knockback
    }

    public enum PlatformGimmickType
    {
        Normal,
        Small
    }
}
